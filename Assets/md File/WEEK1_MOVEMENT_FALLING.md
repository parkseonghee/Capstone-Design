# 리사이클 라이프 — Week 1: 노드 이동 & 쓰레기 낙하

> 이번 주 목표: **플레이어가 그리드 노드를 이동하고, 움직일 때마다 쓰레기(낙하 블록)가 떨어지는** 코어 루프의 뼈대.
> 전투·연쇄는 이번 주 범위 아님 (→ `CORE_COMBAT.md`, 다음 단계). 여기선 쓰레기 = **부딪히면 막히는 낙하 블록**으로만 취급.
> 엔진: Unity 6 / C# · 타깃: Android.

---

## 0. 이번 주 완료 기준 (Definition of Done)

- [ ] 그리드 위에 플레이어가 있고, 4방향으로 한 칸씩 이동한다.
- [ ] 플레이어가 **한 번 움직일 때마다** 쓰레기가 한 스텝 낙하하고, 상단에 새 쓰레기가 스폰된다.
- [ ] 보드가 꽉 차거나 플레이어가 깔리면 게임오버 판정이 뜬다.
- [ ] 위 로직이 **EditMode 테스트로 검증**된다 (연출·아트 없이 순수 로직 먼저).

> 이번 주는 "재미"가 아니라 "루프가 결정론적으로 도는가"를 본다. 밸런스·이펙트는 나중.

---

## 1. 데이터 모델 (이번 주 최소)

```
Grid        : Cell[cols, rows]     // 크기는 GridConfig(SO)에서 읽음. 기본 8 x 12 (모바일 세로). 리터럴 금지
Cell        : Entity? occupant     // null = 빈 칸
Entity      : abstract { EntityKind Kind }
  ├ Player  : { Vector2Int pos }
  └ Trash   : { TrashType type }   // 이번 주엔 type = 색/모양 구분용 뿐, hp/전투 없음

enum EntityKind { Player, Trash }
enum TrashType  { A, B, C, D }     // PCG 전까지 임시. 스폰 시 랜덤
enum Direction  { Up, Down, Left, Right }
```

- 좌표 `Vector2Int(col, row)`, 좌상단 원점, **row 증가 = 아래**.
- 나중에 `Trash`에 material/hp/attack이 붙어 `Enemy`로 확장됨 → 지금은 필드 최소화.

---

## 2. 한 스텝의 순서 (핵심)

플레이어 입력 1회 = 아래 페이즈를 **이 순서대로** 1번씩:

```
Step(Direction dir):
  1. TryMovePlayer(dir)     // 유효하면 이동, 막히면 제자리(그래도 스텝은 진행)
  2. ApplyGravity()         // 떠 있는 쓰레기를 아래로 정착
  3. SpawnTrash()           // 상단에 새 쓰레기 투입
  4. CheckGameOver()        // 꽉 참 / 플레이어 깔림
```

> ⚠️ 이동 실패해도 2~4는 돈다 (원작의 "가만히 있어도 압박이 온다" 감각). 이건 이번 주 결정값.

---

## 3. 이동 규칙 (TryMovePlayer)

```
target = player.pos + dir
if !InBounds(target):        return  // 경계 밖 → 이동 없음
if cellAt(target) is empty:  MovePlayer(target)
else (Trash 있음):           return  // 이번 주엔 그냥 막힘
                                     // → 다음 단계에서 CombatSystem.ResolveMove로 교체
```

---

## 4. 중력 / 낙하 (ApplyGravity)

**컬럼 정착 방식**: 각 컬럼에서 쓰레기가 아래 빈 칸으로 떨어져 바닥/다른 블록 위에 쌓인다. 플레이어는 낙하 대상 아님(플레이어 아래로는 통과/정지 규칙은 §7 미확정).

```
ApplyGravity():
  for each column c:
    아래에서 위로 스캔하며 Trash들을 가능한 최하단 빈 칸으로 압축 이동
  (한 번에 완전 정착시킴 — 애니메이션은 View가 보간)
```

- 로직은 즉시 완전 정착. **떨어지는 연출은 렌더 계층이 보간**해서 보여준다(로직/뷰 분리).

---

## 5. 스폰 (SpawnTrash)

```
SpawnTrash():
  상단 row(0)의 빈 칸 중 N개(기본 1~2, 상수)를 골라 랜덤 TrashType로 채움
  스폰할 빈 칸이 없으면 → 그 자체가 게임오버 신호가 될 수 있음(§6)
```

- 스폰 개수·위치 규칙은 PCG 전까지 **하드코딩 상수**. 나중에 스폰 테이블로 대체.

---

## 6. 게임오버 (CheckGameOver)

- 보드에 빈 칸이 0개 → 오버플로 패배.
- 스폰이 플레이어 위치를 덮으려 함 / 플레이어가 완전히 갇힘 → 패배(정확한 규칙은 §7).

---

## 7. 결정된 값 (Week1 기본값 — 모두 인스펙터/SO 노출, 리터럴 금지)

1. **플레이어 위 낙하 → 막힘.** 플레이어가 벽 역할, 게임오버 아님.
2. **무효 입력(경계 밖) → 무시, 보드 진행 안 함. 유효 이동만 진행.**
   - `advanceOnBlocked`(bool) 인스펙터 노출 — 기본 `false`. 나중에 "막혀도 압박 진행" 실험용.
3. **스텝당 스폰 개수 = 1** (`SpawnConfig`/`GridConfig` 값, 상수 금지).
4. **보드 크기 = 8 × 12 (cols × rows)** — `GridConfig`(SO)에서 읽는다.
   - 폭 8은 원작·연쇄 체감 유지, 높이 12는 모바일 세로 여유. 팀이 8×8 원하면 **SO 값만 변경**(코드 수정 X).

---

## 8. 구현 순서 체크리스트

- [ ] `Grid` + `Cell` + `InBounds` / `Neighbors` 유틸
- [ ] `Player` 배치 + `TryMovePlayer` (쓰레기 없이 이동만)
- [ ] `ApplyGravity` 컬럼 정착 + **테스트**(떠 있는 블록이 바닥에 쌓이는가)
- [ ] `SpawnTrash` 상단 투입
- [ ] `Step()` 4페이즈 결합 + `CheckGameOver`
- [ ] EditMode 테스트: 이동 / 낙하정착 / 스폰누적 / 꽉참-게임오버
- [ ] (그 다음) MonoBehaviour View로 그리드 렌더 + 터치 입력 연결

---

## 부록: Claude Code에게 이 문서를 줄 때

- 이 파일 + 얇은 `CLAUDE.md`(스택/컨벤션/가드레일)만 컨텍스트로 준다.
- **로직 계층(테스트 가능)과 MonoBehaviour/View를 분리**해 달라고 명시.
- "§7 미확정 항목은 임의로 정하지 말고 물어봐라"라고 지시.

---

## 🚫 Claude Code — 하지 말아야 할 것 (Hard Rules)

> 이 규칙은 개별 요청보다 **우선**한다. 규칙을 어기게 되는 상황이면, 코드를 짜지 말고 **먼저 물어봐라.**

### 팀 지정 규칙
1. **하드코딩 금지.** 인스펙터에서 조절 가능한 값(이동 속도, 중력, 스폰 개수, 보드 크기 등)을 코드 리터럴로 박지 말 것.
   → `[SerializeField] private` 필드 또는 `ScriptableObject`로 노출.
   (나쁜 예: 코드에 `gravity = 0;`을 고정 — 인스펙터에서 못 바꿈)
2. **UI 강제 고정 금지.** 게임 시작 시 **코드로 UI 위치·앵커·해상도·레이아웃을 강제 세팅/잠그지 말 것.** 나중에 수정이 매우 어려워진다.
   → 배치는 씬/프리팹/앵커로, 코드는 상태 전환·데이터 바인딩만.
3. **확장 가능한 구조로 짤 것.** 나중에 추가/교체될 여지가 있는 부분(적 종류, 재질, 스폰 규칙, 아이템 등)은 `enum switch` 떡칠·`if` 분기 대신 **인터페이스 / 데이터(SO) / 전략 패턴**으로 갈아끼울 수 있게.
4. **God object 금지.** `GameManager` 하나에 전부 모으고 인스펙터에 다 꽂는 구조 거부. **책임별로 클래스를 나누고**, 참조는 이벤트/주입으로 연결. (찾기 어렵고 지저분함)

### 추가 규칙 (Unity·모바일 실무 — Claude가 제안, 팀 검토)
5. **로직 ↔ MonoBehaviour 분리.** 게임 규칙을 `Update()`나 MonoBehaviour 안에 섞지 말 것. **순수 C# 로직(테스트 가능) + 얇은 View 계층**으로 분리.
6. **취약한 참조 금지.** `GameObject.Find`, `FindObjectOfType`, `Resources.Load("문자열경로")` 남발 금지. → 직렬화 참조·이벤트·경량 DI로 연결.
7. **Singleton/static 남발 금지.** 전역 상태를 여기저기 만들지 말 것(4번의 변형). 꼭 필요한 경우만, 이유를 주석으로 남기고.
8. **핫패스 할당 금지(Android 타깃).** 매 프레임 `Update`·전투 루프에서 `new`, LINQ, 문자열 결합 등 GC 유발 코드 금지. 컬렉션은 재사용.
9. **요청 안 한 대규모 변경 금지.** 시키지 않은 전면 리팩터, 파일·씬 삭제, 폴더 구조 변경 금지. **하기 전에 반드시 확인.**
10. **임의 패키지/에셋 추가 금지.** 서드파티 패키지·에셋 설치는 먼저 물어볼 것.
11. **미확정(⚠️) 항목 임의 확정 금지.** 문서의 "미확정 / 기획 확인 필요" 항목을 스스로 정하지 말고 질문.
12. **검증 없이 "완료" 선언 금지.** 최소 EditMode 테스트 또는 재현 방법을 함께 제시.